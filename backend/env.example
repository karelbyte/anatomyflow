# PostgreSQL (anatomydb) - persistencia de proyectos, schemas, jobs y grafos.
DATABASE_URL=postgresql://user:password@localhost:5432/anatomydb
# Opcional, si se construye la URL en código:
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=user
# POSTGRES_PASSWORD=password
# POSTGRES_DB=anatomydb

# Neo4j (opcional). Si no se configura, el grafo solo se guarda en Postgres/archivo.
# Para una sola instancia usa bolt://; neo4j:// es para clusters (routing).
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=tu_password
# Base de datos Neo4j a usar (Neo4j 4+ permite varias; por defecto "neo4j")
NEO4J_DATABASE=neo4j

# Raíz del explorador de carpetas (Browse). Si no se define, se usa el directorio de trabajo del backend.
# Ejemplo Windows: BROWSER_ROOT=D:\
# Ejemplo Linux: BROWSER_ROOT=/home
# BROWSER_ROOT=

# Repositorios Git clonados (proyectos con GitHub). Por defecto: backend/repos
# REPOS_DIR=

# Token de GitHub para repos privados (se inyecta en la URL al clonar por HTTPS)
# GITHUB_TOKEN=

# GitHub OAuth (recomendado para SaaS: cada proyecto vincula su propia cuenta GitHub)
# Crear OAuth App en https://github.com/settings/developers
# Authorization callback URL debe ser: {BACKEND_URL}/api/auth/github/callback
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GITHUB_REDIRECT_URI=http://localhost:8000/api/auth/github/callback
# URL del frontend (a donde redirigir tras conectar GitHub)
# FRONTEND_URL=http://localhost:5173
